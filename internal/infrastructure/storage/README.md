# Storage
This is a directory for setting up work with different database management systems(Postgres, MySQL, MongoDB, etc.)
Additionally, here we store migrations for chosen DB and also create needed errors that can occur while working with it.