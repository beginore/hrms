# SERVICE
 Layer 2 (service) will be the largest one - the business logic layer, where we write absolutely all logic.
 Service can take different service methods and only its repository methods.
 Converts parts of Domain to needed Models.