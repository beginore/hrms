# COMMAND CENTER (entry points)
Main application for this project<br>
Don't put a lot of code here. If you think that the code can be imported and used in other projects, then it should be stored in `/pkg` directory.<br>
If the code is not reusable or if you don't want others to reuse it, put that code in the `/internal` directory.<br>